define([], function(){
    'use strict';

    var ValueType = {
        ORIGNAL_VALUE: 'originalValue',
        VALUE: 'value',
        DISPLAY_VALUE: 'displayValue'
    };

    return ValueType;
});
