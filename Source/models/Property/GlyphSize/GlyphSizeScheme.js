/*global define*/
define([
        'knockout',
        'jquery'
    ],function(
        ko,
        $){
    'use strict';

    var GlyphSizeScheme = function(state) {

    };

    GlyphSizeScheme.prototype.getType = function(){

    };

    return GlyphSizeScheme;
});