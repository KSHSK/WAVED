define([
        'util/defined',
        'knockout'
    ],function(
        defined,
        ko
    ){
    'use strict';

    var ColoringScheme = function(state) {

    };

    return ColoringScheme;
});